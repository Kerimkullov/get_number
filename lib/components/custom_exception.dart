import 'package:flutter/cupertino.dart';

class CustomErr extends StatelessWidget {
  const CustomErr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ошибка"),
    );
  }
}
